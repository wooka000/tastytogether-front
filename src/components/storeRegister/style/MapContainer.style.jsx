import styled from 'styled-components';

export const MapContainer = styled.div`
    display: flex;
    justify-content: center;
    align-items: center;
        #map{
            width: 850px;
            height: 360px;
        }
        button{
            z-index: 999;
        }

`